---
interface Props {
  direction?: "ltr" | "rtl";
  speed?: number | string;
  className?: string;
}

const { direction = "ltr", speed = 40, className = "" } = Astro.props;
const speedValue = Number(speed);
const content = await Astro.slots.render("default");
---

<div
  class={className}
  data-marquee
  data-direction={direction}
  data-speed={speedValue}
  aria-hidden="true"
>
  <div class="inline-flex will-change-transform" data-marquee-track>
    <div class="inline-flex items-center whitespace-nowrap" set:html={content}></div>
    <div class="inline-flex items-center whitespace-nowrap" set:html={content}></div>
  </div>
</div>
